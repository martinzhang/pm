"""
数据访问层（repositories）-- 薄，按表拆分，全仓库共享

职责边界（只做这一件事）：把「SQL」和「表/列的形状」关在这一层。
每个模块只认识一张表，函数签名是 (conn, 原始值...)，返回 dict / 行 / 基本类型；
不含业务判断、消息文案、字段解析——那些属于各自的领域层（如 wecom/notify.py）。

为什么独立成顶层包、而非塞进 wecom/：
- 这些表（users / tasks / alerts）是全系统的，不属于企业微信。放 wecom/ 下会让
  「谁都可能用的数据访问」被误当成企微私有，别的入口（Web、定时任务）想复用时别扭。
- 一表一文件：改某张表的读写只动一个文件，SQL 不再散落在各 f-string 里。

约定：
- 只接收 conn，不自己 get_db()/关连接——事务边界由调用方掌握（一次请求可能跨多表）。
- 写操作内部 commit；是否 commit 属于「这条写算不算完成」的数据语义，留在本层。
"""
